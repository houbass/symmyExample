import type { DataItem } from "../../../global/types";
interface Props {
    index: number;
    item: DataItem;
}
declare function ProductCard({ index, item }: Props): import("react/jsx-runtime").JSX.Element;
export default ProductCard;
